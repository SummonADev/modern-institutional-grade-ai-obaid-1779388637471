# AlphaEdge Platform Environment Variables
# Add your API keys below — never commit real keys

# No VITE_* environment variables are required for the client-side build.
# API keys are stored securely in localStorage after user onboarding.
